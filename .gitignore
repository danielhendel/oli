# ----------------------------------------
# Dependencies / Package managers
# ----------------------------------------
node_modules/
node_modules/.cache/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ----------------------------------------
# Expo / React Native
# ----------------------------------------
.expo/
dist/
web-build/
expo-env.d.ts

# ----------------------------------------
# TypeScript build outputs (MONOREPO SAFE)
# ----------------------------------------
dist-types/
lib/dist-types/
services/**/dist-types/

# tsbuildinfo (project references / incremental builds)
*.tsbuildinfo
node_modules/.cache/tsbuildinfo/

# ----------------------------------------
# Metro / packager caches
# ----------------------------------------
.metro-health-check*

# ----------------------------------------
# Native (iOS / Android)
# ----------------------------------------
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.keystore

# Android build output
android/**/build/
android/.gradle/

# iOS build output & local env
ios/Pods/
ios/**/Pods/
ios/DerivedData/
ios/build/
ios/.xcode.env.local
*.xcuserstate
**/xcuserdata/
*.hmap
*.ipa
*.dSYM

# ----------------------------------------
# Logs / Debug
# ----------------------------------------
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Firebase / emulator debug logs (never commit)
firebase-debug.log
firestore-debug.log
ui-debug.log

# ----------------------------------------
# macOS
# ----------------------------------------
.DS_Store

# ----------------------------------------
# Environment variables (CRITICAL)
# ----------------------------------------
.env
.env.*
!.env.example

# ----------------------------------------
# Local archives / audit artifacts
# ----------------------------------------
*.zip
oli-project-src/
oli-project-src.zip
oli-review.zip
oli-native-audit.zip
oli-*.diff
oli-*.txt
AUDIT_CONTEXT.txt

# ----------------------------------------
# Legacy / local-only folders
# ----------------------------------------
archive/
docs/_archive/

# ----------------------------------------
# Firebase local tooling files
# ----------------------------------------
.firebase/

# ⚠️ IMPORTANT:
# These are environment-specific.
# Commit ONLY if you intentionally want repo-bound Firebase config.
# ----------------------------------------
# firebase.json
# .firebaserc

# ----------------------------------------
# Firebase / Functions build output
# ----------------------------------------
services/functions/lib/

# ----------------------------------------
# Cloud Run local env (never commit)
# ----------------------------------------
cloudrun/.env.local
services/api/.env.local

# Never commit built JS next to TS sources
lib/contracts/*.js
lib/contracts/*.js.map
