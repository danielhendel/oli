# ----------------------------------------
# Dependencies / Package managers
# ----------------------------------------
node_modules/
node_modules/.cache/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# ----------------------------------------
# Expo / React Native
# ----------------------------------------
.expo/
dist/
dist-types/
web-build/
expo-env.d.ts

# Metro / packager caches
.metro-health-check*

# ----------------------------------------
# Native (iOS / Android)
# ----------------------------------------
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.keystore

# Android build output
android/**/build/
android/.gradle/

# iOS build output & local env
ios/Pods/
ios/DerivedData/
ios/.xcode.env.local
*.xcuserstate
*.xcuserdata
*.hmap
*.ipa
*.dSYM

# ----------------------------------------
# Logs / Debug
# ----------------------------------------
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Firebase / emulator debug logs (never commit)
firebase-debug.log
firestore-debug.log
ui-debug.log

# ----------------------------------------
# macOS
# ----------------------------------------
.DS_Store

# ----------------------------------------
# Environment variables (CRITICAL)
# ----------------------------------------
.env
.env.*
!.env.example

# ----------------------------------------
# TypeScript / build caches
# ----------------------------------------
*.tsbuildinfo
node_modules/.cache/tsbuildinfo/

# ----------------------------------------
# Local archives / audit artifacts
# ----------------------------------------
*.zip
oli-project-src/
oli-project-src.zip
oli-review.zip
oli-native-audit.zip
oli-*.diff
oli-*.txt

# ----------------------------------------
# Legacy / local-only folders
# ----------------------------------------
archive/
docs/_archive/

# ----------------------------------------
# Firebase local tooling files (staging-only repo hygiene)
# ----------------------------------------
.firebase/
firebase.json
.firebaserc

# ----------------------------------------
# Firebase / Functions build output
# ----------------------------------------
services/functions/lib/

# ----------------------------------------
# Cloud Run local env (never commit)
# ----------------------------------------
cloudrun/.env.local
services/api/.env.local
AUDIT_CONTEXT.txt
