// Cardio product landing — mirrors Strength flow; data filtered to cardio-only in TrainingOverviewScreen.
import { TrainingOverviewScreen } from "../workouts/overview";

export default function CardioOverviewScreen() {
  return <TrainingOverviewScreen domain="cardio" />;
}
